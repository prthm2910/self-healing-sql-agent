---
phase: 06-divide-and-conquer
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: [src/workflow/nodes.py, src/workflow/builder.py]
autonomous: true
requirements: [DAC-03, DAC-04]
must_haves:
  truths:
    - "The graph routes complex queries through the decomposition loop."
    - "The assembler node produces a final executable SQL query using the SQLTranspiler."
    - "Complex Pagila questions are answered correctly via the Divide & Conquer workflow."
  artifacts:
    - path: "src/workflow/nodes.py"
      provides: "assembler_node and loop routing helpers"
    - path: "src/workflow/builder.py"
      provides: "Updated LangGraph orchestration"
  key_links:
    - from: "src/workflow/builder.py"
      to: "src/workflow/nodes.py"
      via: "assembler_node and conditional edges"
---

<objective>
Complete the Divide & Conquer SQL Factory by implementing the Assembler node and orchestrating the multi-agent loop in LangGraph.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/workflow/nodes.py
@src/workflow/builder.py
@src/services/sql_engine.py
@.planning/phases/06-divide-and-conquer/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Implement Assembler Node</name>
  <files>src/workflow/nodes.py</files>
  <action>
    - Implement `assembler_node(state: State)`:
      - Call `SQLTranspiler.merge_snippets(state['sql_snippets'], state['join_plan'])`.
      - Store the result in `state['current_sql']`.
      - Log the final query and the assembly thought process to `agent_logs`.
    - Implement `subtask_iterator_node(state: State)`:
      - This node simply increments `current_task_index` and checks if more tasks remain.
  </action>
  <verify>
    <automated>python -c "from src.workflow.nodes import assembler_node; print('Assembler node success')"</automated>
  </verify>
  <done>Assembler node is implemented and integrates with SQLTranspiler.</done>
</task>

<task type="auto">
  <name>Orchestrate LangGraph Loop</name>
  <files>src/workflow/builder.py</files>
  <action>
    - Update `classifier_router` to route to `decomposer` if `is_complex` is true.
    - Add `decomposer`, `worker`, `healer`, `assembler`, and `subtask_iterator` nodes to the graph.
    - Define the loop logic:
      1. `decomposer` -> `worker`.
      2. `worker` -> `healer` (if `sql_error`) OR `subtask_iterator`.
      3. `healer` -> `worker` (retry).
      4. `subtask_iterator` -> `worker` (if `current_task_index < len(sub_tasks)`) OR `assembler`.
      5. `assembler` -> `execute_sql`.
    - Update existing edges to ensure `execute_sql` still flows to the standard formatting/synthesis logic.
    - Implement the `worker_router` to handle the conditional branching for healing.
  </action>
  <verify>
    <automated>python -c "from src.workflow.builder import sql_chatbot_graph; print('Graph compilation success')"</automated>
  </verify>
  <done>LangGraph is updated to support the full multi-agent decomposition and assembly loop.</done>
</task>

<task type="auto">
  <name>Integration Testing for Pagila Scenarios</name>
  <files>tests/test_decomposition.py</files>
  <action>
    - Create `tests/test_decomposition.py`.
    - Write integration tests for complex scenarios (e.g., "Top 5 customers by revenue who rented Horror movies in 2005").
    - Verify that:
      - `is_complex` is correctly set.
      - Decomposer produces multiple sub-tasks.
      - Final SQL executes successfully and returns expected data.
  </action>
  <verify>
    <automated>pytest tests/test_decomposition.py</automated>
  </verify>
  <done>Full workflow is verified against complex real-world database queries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Assembler -> SQL Engine: The final merged SQL is passed to the engine for execution.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | assembler_node | mitigate | The deterministic assembly process via SQLGlot ensures that no logic outside the Join Plan is injected into the final query. |
</threat_model>

<success_criteria>
- Complex queries are handled by the decomposition workflow.
- `agent_logs` contains the full provenance of the "Divide & Conquer" process.
- Performance on complex queries is significantly more reliable than the single-shot approach.
</success_criteria>

<output>
After completion, create `.planning/phases/06-divide-and-conquer/06-03-SUMMARY.md`
</output>
